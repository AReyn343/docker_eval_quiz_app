# quiz-app

La commande permet le lancement de l'application.
```bash
docker compose up -d
```
La page d'accueil s'afffiche correctement à l'adresse
```bash
http://localhost:8087/
```

Les images docker sont créées, mais il y a un problème avec le back end que je n'arrive pas à identifier et qui ne permet pas l'inscription au quiz